# Versiones fijadas para que el proyecto sea reproducible.
# Si añadís una librería, añadidla aquí con su versión.
pandas==2.2.3
numpy==2.1.3
matplotlib==3.9.2
scikit-learn==1.5.2
spacy==3.8.2
nltk==3.9.1
requests==2.32.3
python-dotenv==1.0.1
jupyter==1.1.1

# El modelo de espanol de spaCy no se instala desde aqui. Una vez, en la terminal:
#   python -m spacy download es_core_news_sm     # taller 01: sin vectores de palabra
#   python -m spacy download es_core_news_md     # taller 02: 20.000 vectores de 300 dim.
# Los notebooks los descargan solos si faltan.
