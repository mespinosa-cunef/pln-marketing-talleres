# Copiad este fichero a .env y poned vuestras claves reales.
# .env está en .gitignore: no se sube nunca al repositorio.
# Si alguna vez subís una clave por error, dadla por comprometida y revocadla.

API_KEY=
API_SECRET=
BEARER_TOKEN=
